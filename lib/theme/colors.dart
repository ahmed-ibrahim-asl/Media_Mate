//-------------------------- flutter_core ------------------------------
import 'package:flutter/material.dart';
//----------------------------------------------------------------------

class AppColors {
  // Brand
  static const primary = Color(0xFF1766B9);
  static const primaryAlt = Color(0xFF327BF1);
  static const secondary = Color(0xFF7A5AF5);

  // Background gradient
  static const bgTop = Color(0xFFEAF9FF);
  static const bgBottom = Color(0xFFFDFEFF);

  // Neutrals / text
  static const textDark = Color(0xFF222B32);
  static const text = Color(0xFF111827);
  static const textMuted = Color(0xFF6B7280);
  static const textSubtle = Color(0xFF606C77);
  static const slate700 = Color(0xFF374151);
  /////////////////////////////////////////////////////////
  // Surfaces & borders
  static const surface = Colors.white;
  static const chip = Color(0xFFE9EEF2);
  static const cardShadow = Color(0x1A000000); // 10% black
  static const cardShadowStrong = Color(0x33000000); // 20% black
  static const border = Color(0xFFE5E7EB);

  // Status
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
}
